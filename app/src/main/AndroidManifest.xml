<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.rafaeluribe.ejemploviewmodelmvvmapi3">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_news"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_news_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EjemploViewModelMVVMAPI3"
        android:usesCleartextTraffic="true"
        tools:targetApi="m">

        <activity
            android:name=".view.activities.VistaDetalleNoticias"
            android:exported="false" />
        <activity
            android:name=".view.activities.Registro"
            android:exported="false"
            android:parentActivityName=".view.activities.Login" />
        <activity
            android:name=".view.activities.MainActivity"
            android:exported="false" />

        <activity
            android:name=".view.activities.Login"
            android:exported="false"/>

        <activity
            android:name=".view.activities.SplashScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>